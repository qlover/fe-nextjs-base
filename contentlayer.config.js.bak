import { makeSource } from "contentlayer/source-files";
import { documentTypes } from "./work/contentlayer-builder";

export default makeSource({
  contentDirPath: "./content",
  documentTypes: documentTypes,
});
